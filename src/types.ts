export interface FoodInterface{
    id: number;
    name: string;
    image: string;
    description: string;
    price: number;
    available: boolean;
};

export interface FoodAdding{
    name: string;
    image: string;
    description: string;
    price: number;
};

